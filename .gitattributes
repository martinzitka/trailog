# Normalise line endings. The Gradle wrapper script must stay LF or it breaks on *nix/CI.
* text=auto eol=lf
*.bat text eol=crlf
gradlew text eol=lf

# Binaries
*.jar binary
*.png binary
*.jpg binary
*.keystore binary
*.jks binary
